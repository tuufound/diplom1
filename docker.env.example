# Скопируйте в файл .env в корне репозитория (рядом с docker-compose.yaml).
# Запуск только на этом ПК в браузере — достаточно значений ниже.

DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,backend
VITE_API_BASE_URL=http://localhost:8000/api
VITE_PROXY_TARGET=http://backend:8000
